[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    coverage
    pytest-cov
    flask
    flask_wtf
commands = pytest --cov app app_test.py --cov-report xml
 
[coverage:run]